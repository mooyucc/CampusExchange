# 🔍 排查应用错误

## 当前状态
- ✅ 数据库表已创建（User, Item, ExchangeRequest）
- ✅ 表名正确（PascalCase）
- ❌ 应用仍然报错（100% 错误率）

## 可能的原因

### 1. Prisma Client 需要重新生成
数据库结构已更新，但 Vercel 上的应用仍在使用旧的 Prisma Client。

### 2. 数据库连接问题
环境变量可能不正确，或连接池配置有问题。

### 3. 列名不匹配
虽然表名正确，但列名可能不匹配。

## 解决步骤

### 步骤 1：查看 Vercel 日志

1. 在 Vercel 项目页面
2. 点击顶部导航的 **Logs** 标签
3. 查看 **Function Logs** 或 **Runtime Logs**
4. 找到最新的错误信息
5. **复制完整的错误信息**

常见的错误可能是：
- `Table "User" does not exist` - 表名问题
- `column "createdAt" does not exist` - 列名问题
- `Connection timeout` - 连接问题
- `PrismaClientInitializationError` - Prisma 初始化问题

### 步骤 2：检查数据库列名

在 Supabase Table Editor 中，点击 `User` 表，检查列名是否完全匹配：

**应该有的列名（驼峰命名，区分大小写）：**
- `id`
- `name`
- `email`
- `password`
- `grade`
- `avatar`
- `createdAt`（不是 `created_at`）
- `updatedAt`（不是 `updated_at`）

如果列名是 `created_at` 或 `updated_at`，需要重命名。

### 步骤 3：触发重新部署

在 Vercel 中：
1. 进入 **Deployments** 标签
2. 找到最新的部署
3. 点击 **Redeploy**（重新部署）

或者，在本地做一个小的更改并推送：
```bash
# 做一个小的更改来触发重新部署
echo "# 数据库初始化完成" >> README.md
git add README.md
git commit -m "数据库初始化完成"
git push origin main
```

### 步骤 4：验证环境变量

在 Vercel 项目设置中，确认：
- `DATABASE_URL` 正确设置（使用连接池 URL，端口 6543）
- `NEXTAUTH_SECRET` 已设置
- `NEXTAUTH_URL` 已设置

---

## 快速检查 SQL

如果列名不匹配，在 Supabase SQL Editor 中运行这个查询来检查：

```sql
-- 检查 User 表的列名
SELECT column_name 
FROM information_schema.columns 
WHERE table_schema = 'public' 
  AND table_name = 'User'
ORDER BY ordinal_position;
```

**应该返回：**
- id
- name
- email
- password
- grade
- avatar
- createdAt
- updatedAt

如果返回的是 `created_at` 或 `updated_at`，则需要修复。

---

## 最可能的问题

根据错误信息 "Application error: a server-side exception has occurred"，最可能是：

1. **列名不匹配** - 最常见
2. **Prisma Client 缓存问题** - 需要重新部署
3. **数据库连接失败** - 检查 DATABASE_URL

**请先查看 Vercel Logs，然后告诉我具体的错误信息！**

