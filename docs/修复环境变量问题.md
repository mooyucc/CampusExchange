# 🔧 修复环境变量问题

## 发现的问题

- ✅ `campus-exchange-l9xd` 项目：已配置环境变量
- ❌ `campus-exchange` 项目：**没有配置环境变量**
- 🌐 实际访问的 URL：`campus-exchange-theta.vercel.app`（这是 `campus-exchange` 项目）

## 解决方案

### 在 `campus-exchange` 项目中添加环境变量

1. **进入项目设置**
   - 在 Vercel 中选择 `campus-exchange` 项目
   - 点击 **Settings** → **Environment Variables**

2. **添加以下三个环境变量：**

#### 变量 1: DATABASE_URL
```
Key: DATABASE_URL
Value: postgresql://postgres:Sbmooyu123@db.amqaeolouwkpsqndrubk.supabase.co:6543/postgres?pgbouncer=true
Environments: All Environments
```

#### 变量 2: NEXTAUTH_SECRET
```
Key: NEXTAUTH_SECRET
Value: +puVPV8IYSYWMccjC/NUggK2mTRo3KTKHNwYUzyaU6U=
Environments: All Environments
```

#### 变量 3: NEXTAUTH_URL（重要！）
```
Key: NEXTAUTH_URL
Value: https://campus-exchange-theta.vercel.app
Environments: All Environments
```

3. **点击 Save**

4. **重新部署**
   - 进入 **Deployments** 标签
   - 找到最新部署
   - 点击 **Redeploy** 按钮

---

## 快速操作步骤

1. ✅ 打开 `campus-exchange` 项目
2. ✅ Settings → Environment Variables
3. ✅ 添加三个环境变量（参考上面的值）
4. ✅ Save
5. ✅ Deployments → Redeploy

---

## 验证

部署完成后：
- ✅ 访问 `https://campus-exchange-theta.vercel.app`
- ✅ 应该不再显示错误
- ✅ 可以正常访问首页

---

## 建议

配置完成后，可以考虑：
- 删除 `campus-exchange-l9xd` 项目（避免混淆）
- 或者将两个项目的环境变量都配置好（如果都需要使用）

