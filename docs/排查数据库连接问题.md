# 🔍 排查数据库连接问题

## 当前错误
```
Can't reach database server at 'db.amqaeolouwkpsqndrubk.supabase.co:5432'
```

## 检查清单

### ✅ 步骤 1：确认密码已更新
- [ ] 在 Vercel 中，`DATABASE_URL` 已更新为：`postgresql://postgres:SbMooyu123@db.amqaeolouwkpsqndrubk.supabase.co:5432/postgres`
- [ ] 密码是 `SbMooyu123`（M 大写）

### ✅ 步骤 2：确认已保存并重新部署
- [ ] 点击了 "Save" 按钮
- [ ] 已重新部署（进入 Deployments → Redeploy）

### ✅ 步骤 3：检查 Supabase 项目状态
在 Supabase 中：
- [ ] 项目是否正常运行？
- [ ] 数据库是否在线？
- [ ] 是否有暂停或限制？

### ✅ 步骤 4：尝试连接池端口
如果直接连接（5432）不行，尝试连接池（6543）：

```
DATABASE_URL=postgresql://postgres:SbMooyu123@db.amqaeolouwkpsqndrubk.supabase.co:6543/postgres?pgbouncer=true
```

### ✅ 步骤 5：检查 IP 白名单
在 Supabase 项目中：
1. Settings → Database
2. 检查 Network Restrictions 或 IP Whitelist
3. 确保允许所有 IP 或添加 Vercel 的 IP

---

## 快速测试连接

在 Supabase SQL Editor 中运行：
```sql
SELECT 1;
```
如果成功，说明数据库本身没问题。

---

## 推荐操作顺序

1. **确认密码已正确更新**（`SbMooyu123`）
2. **保存环境变量**
3. **重新部署**
4. **等待 2-3 分钟**
5. **检查新的日志**

如果仍然失败，尝试：
- 使用连接池端口 6543
- 检查 Supabase 项目设置

