# 🔧 修复表名问题

## ⚠️ 发现的问题

您的 Table Editor 中显示的表名是：
- `users`（小写）
- `items`（小写）
- `exchange_requests`（小写）

但是 Prisma 期望的表名是：
- `User`（PascalCase，带双引号）
- `Item`（PascalCase，带双引号）
- `ExchangeRequest`（PascalCase，带双引号）

**这会导致 Prisma 找不到表，应用仍然会报错！**

---

## ✅ 解决方案

### 方法 1：重新创建表（推荐）

如果表里还没有数据，最简单的方法是：

1. **在 Supabase SQL Editor 中运行：**
```sql
-- 删除旧表（如果存在）
DROP TABLE IF EXISTS "exchange_requests" CASCADE;
DROP TABLE IF EXISTS "items" CASCADE;
DROP TABLE IF EXISTS "users" CASCADE;

-- 然后重新运行 prisma/init-database.sql 中的 SQL
```

2. **然后重新运行 `prisma/init-database.sql` 中的完整 SQL**

### 方法 2：重命名表

如果表中已有数据，需要重命名：

1. **在 Supabase SQL Editor 中运行：**

```sql
-- 重命名表
ALTER TABLE "users" RENAME TO "User";
ALTER TABLE "items" RENAME TO "Item";
ALTER TABLE "exchange_requests" RENAME TO "ExchangeRequest";
```

2. **同时需要重命名列名（如果列名是小写的）：**

检查列名是否匹配。Prisma 期望的列名：
- `createdAt`（驼峰命名）
- `updatedAt`（驼峰命名）
- `ownerId`（驼峰命名）
- 等等

如果列名是 `created_at`、`updated_at`、`owner_id`（snake_case），需要重命名：

```sql
-- 重命名 User 表的列
ALTER TABLE "User" RENAME COLUMN "created_at" TO "createdAt";
ALTER TABLE "User" RENAME COLUMN "updated_at" TO "updatedAt";

-- 重命名 Item 表的列
ALTER TABLE "Item" RENAME COLUMN "created_at" TO "createdAt";
ALTER TABLE "Item" RENAME COLUMN "updated_at" TO "updatedAt";
ALTER TABLE "Item" RENAME COLUMN "owner_id" TO "ownerId";

-- 重命名 ExchangeRequest 表的列
ALTER TABLE "ExchangeRequest" RENAME COLUMN "created_at" TO "createdAt";
ALTER TABLE "ExchangeRequest" RENAME COLUMN "updated_at" TO "updatedAt";
ALTER TABLE "ExchangeRequest" RENAME COLUMN "requester_id" TO "requesterId";
ALTER TABLE "ExchangeRequest" RENAME COLUMN "receiver_id" TO "receiverId";
ALTER TABLE "ExchangeRequest" RENAME COLUMN "item_id" TO "itemId";
```

---

## 🎯 推荐操作

**如果表是空的（还没有数据），最简单的方法：**

1. 在 Supabase SQL Editor 中运行：
```sql
DROP TABLE IF EXISTS "exchange_requests" CASCADE;
DROP TABLE IF EXISTS "items" CASCADE;
DROP TABLE IF EXISTS "users" CASCADE;
```

2. 然后重新运行 `prisma/init-database.sql` 中的完整 SQL（确保使用双引号包裹表名）

3. 验证表名：
   - 在 Table Editor 中应该看到 `User`、`Item`、`ExchangeRequest`（PascalCase）

---

## ✅ 完成后验证

1. 表名应该是：`User`、`Item`、`ExchangeRequest`
2. 访问 Vercel 应用，应该能正常工作
3. 错误率应该降为 0%

**请按照上述步骤修复表名！**

